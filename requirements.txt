# Pinned to the last releases that still support Python 3.9, which is the
# system Python on the demo machine. Newer FastAPI/uvicorn require >=3.10.
fastapi==0.128.8
uvicorn==0.39.0
starlette==0.49.3
pydantic>=2.9,<3

# Deliberately NOT depending on python-multipart: the browser reads photos with
# FileReader and posts them as base64 JSON, which is also the format real vision
# APIs expect. See backend/ai/openai_provider.py.
